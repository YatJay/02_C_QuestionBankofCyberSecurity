/*古代有一个梵塔，塔内有3个座A、B、C。初始时A座上有n个盘子，盘子大小不等，大的在下，小的在上。
	有一个老和尚想把这n个盘子从A座移动到C座，但是每次只允许移动一个盘子，而且在移动过程中在3个座上需要始终保持大盘在下，小盘在上，在移动过程中可以使用B座。
	要求使用递归的方法模拟并输出移动的步骤。 */
	
/*
汉诺塔问题的递推式为：F(n)=2F(n-1)+1
*/
	
#include <stdio.h>
int main(void){
    
    
    return 0;
}
